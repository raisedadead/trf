---
import "@fontsource-variable/inter";
import "../index.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { seoForPath, OG_IMAGE, SITE_NAME, SITE_URL, INITIATIVE } from "../lib/seo.ts";

const seo = seoForPath(Astro.url.pathname);

const orgLd = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: SITE_NAME,
  url: SITE_URL,
  logo: OG_IMAGE,
  description: `${INITIATIVE}, run by volunteers, funding FOSS in India through small monthly contributions.`,
  disambiguatingDescription: `${INITIATIVE}. Run by community volunteers.`,
};

// Google reads the site name from WebSite data on the home page only.
const websiteLd = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  name: SITE_NAME,
  url: SITE_URL,
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#057a33" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="canonical" href={seo.canonical} />
    <title>{seo.title}</title>
    <meta name="description" content={seo.description} />
    <meta property="og:site_name" content={SITE_NAME} />
    <meta property="og:title" content={seo.ogTitle} />
    <meta property="og:description" content={seo.ogDescription} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={seo.canonical} />
    <meta property="og:image" content={OG_IMAGE} />
    <meta
      property="og:image:alt"
      content="The Rupee Fund — a community initiative from FOSS United"
    />
    <meta name="twitter:card" content="summary_large_image" />
    {!seo.indexable && <meta name="robots" content="noindex" />}
    <script is:inline type="application/ld+json" set:html={JSON.stringify(orgLd)} />
    {
      seo.path === "/" && (
        <script is:inline type="application/ld+json" set:html={JSON.stringify(websiteLd)} />
      )
    }
  </head>
  <body class="bg-paper text-ink font-sans min-h-screen flex flex-col">
    <a
      href="#main"
      class="sr-only focus:not-sr-only focus:absolute focus:z-[100] focus:left-4 focus:top-4 focus:rounded-lg focus:bg-brand-fg focus:px-4 focus:py-2 focus:text-white focus:font-semibold"
    >
      Skip to content
    </a>
    <Header />
    <main id="main" tabindex="-1" class="flex-1 screen-enter">
      <slot />
    </main>
    <Footer />
  </body>
</html>
