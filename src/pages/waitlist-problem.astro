---
import Base from "../layouts/Base.astro";
import { REMOVAL_ADDRESS } from "../lib/contact.ts";
---

<Base>
  <div class="min-h-screen screen-enter">
    <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-16 text-center">
      <h1 class="text-2xl font-bold text-ink tracking-tight mb-3">That didn't go through</h1>
      <p class="text-sm text-ink-2 mb-2">
        We couldn't add you to the list. Check the email address and try once more.
      </p>
      <p class="text-sm text-ink-2 mb-6">
        If you tried a few times in quick succession, wait a minute before trying again.
      </p>
      <p class="text-xs text-ink-3 mb-8">
        Still stuck? Email <a href={`mailto:${REMOVAL_ADDRESS}`} class="underline hover:text-brand-fg">{REMOVAL_ADDRESS}</a> and we'll add you by hand.
      </p>
      <a
        href="/subscribe"
        class="bg-brand-fg text-white px-6 py-3 rounded-lg font-semibold hover:bg-brand-900 text-sm transition duration-150 ease-out active:scale-[0.98] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-fg focus-visible:ring-offset-2 focus-visible:ring-offset-white"
      >
        Try again
      </a>
    </div>
  </div>
</Base>
